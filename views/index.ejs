<%- include('head') %>
<% if (messages.length) { %>
    <% messages.forEach((message) => { %>
         <h1><%= message.text %></h1>
        <p><%= message.username %></p>
        <p><%= message.added %></p>
        <% }); %>
 <% } %>